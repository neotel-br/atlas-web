services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "${DEV_PORT:-3000}:3000"
    environment:
      NEXT_TELEMETRY_DISABLED: "1"
    stdin_open: true
    tty: true
    volumes:
      - .:/app
      - atlas_node_modules:/app/node_modules
      - atlas_next_cache:/app/.next

volumes:
  atlas_node_modules:
  atlas_next_cache:
